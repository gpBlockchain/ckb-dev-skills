[package]
name = "red-packet-type"
version = "0.1.0"
edition = "2024"

[dependencies]
ckb-std = { version = "1.1", features = ["type-id"] }
red-packet-common = { path = "../../crates/red-packet-common" }

[features]
library = []
native-simulator = ["library", "ckb-std/native-simulator"]
